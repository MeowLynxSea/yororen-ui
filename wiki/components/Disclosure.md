# Disclosure

A disclosure arrow (expanded/collapsed) visual primitive.

## Example

```rust
use yororen_ui::component::disclosure;

let collapsed = disclosure().expanded(false);
let expanded = disclosure().expanded(true);
```
