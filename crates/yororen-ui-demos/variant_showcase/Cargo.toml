[package]
name = "variant-showcase-demo"
version = "0.1.0"
edition.workspace = true
# Demo / example binary — never published to crates.io.
publish = false

[dependencies]
gpui = { package = "gpui-ce", version = "0.2.2" }
gpui-ce-platform = { package = "gpui_ce_platform", version = "0.1" }
yororen-ui = { package = "yororen_ui", path = "../../yororen-ui" }


[lints]
workspace = true
